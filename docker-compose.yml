version: '3.8'

services:
  validador:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: validador
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=development
